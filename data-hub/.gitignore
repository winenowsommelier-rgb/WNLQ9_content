venv/
__pycache__/
*.pyc
.pytest_cache/

# Google service-account credentials (secret -- never commit)
config/google-credentials.json

# Runtime logs (keep the directory via logs/.gitkeep, ignore log files)
logs/*
!logs/.gitkeep
