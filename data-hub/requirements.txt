feedparser>=6.0.10
requests>=2.31.0
beautifulsoup4>=4.12.0
python-dateutil>=2.8.2
pyyaml>=6.0
pytest>=7.4.0
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
# Optional Supabase/Postgres backend (env-gated via DATA_HUB_DB_BACKEND).
# The import is lazy, so the test suite still passes if this isn't installed.
supabase>=2.0.0
